"""
No models yet (docs/DATABASE_DESIGN.md "analytics"). Aggregated rollup
tables are added in Phase 14 once real query/traffic patterns from the
other apps exist to aggregate — adding them speculatively now would be
guessing at a schema (spec §56 "avoid premature abstraction").
"""
